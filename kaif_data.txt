<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>For Sara ❤️</title>
<link rel="stylesheet" href="style.css">
</head>
<body>

<div class="intro">
  <h1 class="intro-text">For Someone Very Special ❤️</h1>
</div>

<div class="container">

  <h1 class="name">Sara <span>urf Ahana</span> ❤️</h1>

  <div id="wish" class="wish"></div>

  <button class="start-btn" onclick="startLove()">Start Surprise ✨</button>

  <div id="dua" class="dua"></div>

  <div class="proposal" id="proposal">
    <h2>Will You Be Mine Forever? 💍</h2>
    <button class="yes" onclick="yesLove()">YES ❤️</button>
    <button class="no" id="noBtn">NO 😅</button>
  </div>

</div>

<canvas id="canvas"></canvas>

<script src="script.js"></script>
</body>
</html>

*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:'Poppins',sans-serif;
}

body{
background:black;
overflow:hidden;
color:white;
text-align:center;
}

.intro{
position:fixed;
width:100%;
height:100%;
background:black;
display:flex;
justify-content:center;
align-items:center;
animation:fadeOut 3s forwards 3s;
z-index:10;
}

@keyframes fadeOut{
to{opacity:0;visibility:hidden;}
}

.container{
position:absolute;
top:50%;
left:50%;
transform:translate(-50%,-50%);
width:90%;
}

.name{
font-size:3.5rem;
text-shadow:0 0 20px #ff0080,0 0 60px #ff4da6;
animation:pulse 2s infinite;
}

.name span{
display:block;
font-size:1.8rem;
color:#ffb3d9;
}

@keyframes pulse{
0%{transform:scale(1);}
50%{transform:scale(1.08);}
100%{transform:scale(1);}
}

.wish{
margin-top:20px;
font-size:1.8rem;
min-height:60px;
color:#ffcce6;
}

.start-btn{
margin-top:20px;
padding:10px 30px;
border:none;
border-radius:30px;
background:linear-gradient(45deg,#ff0080,#ff4d4d);
color:white;
cursor:pointer;
font-size:1.1rem;
transition:.3s;
}

.start-btn:hover{transform:scale(1.1);}

.dua{
margin-top:25px;
font-size:1.2rem;
white-space:pre-line;
min-height:100px;
}

.proposal{
margin-top:30px;
display:none;
}

.proposal h2{
margin-bottom:15px;
}

.yes,.no{
padding:10px 25px;
margin:10px;
border:none;
border-radius:25px;
cursor:pointer;
font-size:1rem;
}

.yes{
background:#ff0080;
color:white;
}

.no{
background:#444;
color:white;
position:relative;
}

canvas{
position:fixed;
top:0;
left:0;
z-index:-1;
}

@media(max-width:768px){
.name{font-size:2.3rem;}
.wish{font-size:1.2rem;}
}

const wish = document.getElementById("wish");
const duaBox = document.getElementById("dua");
const proposal = document.getElementById("proposal");
const noBtn = document.getElementById("noBtn");

/* Wish Animation */

setTimeout(()=>{
wish.innerHTML="🎂 Happy Birthday My Love ❤️";
},4000);

setTimeout(()=>{
wish.innerHTML+="<br>💖 Valentine Mubarak 💖";
},7000);

/* Start Surprise */

function startLove(){
typeText("Dear Sara urf Ahana ❤️\n\nAllah tumhe har khushi de.\nTumhari zindagi roshan rahe.\nTum meri dua ho.\n\n– Mohd Kaif ❤️");
launchHearts();
setTimeout(()=>proposal.style.display="block",5000);
}

/* Typing Effect */

function typeText(text){
duaBox.innerHTML="";
let i=0;
function typing(){
if(i<text.length){
duaBox.innerHTML+=text.charAt(i);
i++;
setTimeout(typing,40);
}
}
typing();
}

/* Moving NO Button */

noBtn.addEventListener("mouseover",()=>{
let x=Math.random()*200-100;
let y=Math.random()*200-100;
noBtn.style.transform=`translate(${x}px,${y}px)`;
});

/* YES Button */

function yesLove(){
launchFireworks();
alert("Yayyy ❤️ Mohd Kaif Loves You Forever 💍✨");
}

/* Heart Rain */

function launchHearts(){
for(let i=0;i<40;i++){
let heart=document.createElement("div");
heart.innerHTML="❤️";
heart.style.position="fixed";
heart.style.left=Math.random()*100+"vw";
heart.style.top=Math.random()*100+"vh";
heart.style.fontSize="20px";
heart.style.animation="float 3s linear forwards";
document.body.appendChild(heart);
setTimeout(()=>heart.remove(),3000);
}
}

/* Fireworks */

const canvas=document.getElementById("canvas");
const ctx=canvas.getContext("2d");
canvas.width=window.innerWidth;
canvas.height=window.innerHeight;

let particles=[];

function launchFireworks(){
for(let i=0;i<150;i++){
particles.push({
x:canvas.width/2,
y:canvas.height/2,
radius:Math.random()*3,
color:`hsl(${Math.random()*360},100%,50%)`,
speedX:(Math.random()-0.5)*8,
speedY:(Math.random()-0.5)*8
});
}
}

function animate(){
ctx.fillStyle="rgba(0,0,0,0.2)";
ctx.fillRect(0,0,canvas.width,canvas.height);

particles.forEach((p,index)=>{
ctx.beginPath();
ctx.arc(p.x,p.y,p.radius,0,Math.PI*2);
ctx.fillStyle=p.color;
ctx.fill();
p.x+=p.speedX;
p.y+=p.speedY;
if(p.radius>0.1)p.radius-=0.02;
if(p.radius<=0.1)particles.splice(index,1);
});

requestAnimationFrame(animate);
}

animate();
